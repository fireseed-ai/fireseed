apiVersion: v1
kind: Service
metadata:
  name: fireseed-ui
  namespace: {{ include "fireseed.namespace" . }}
  labels:
    app: fireseed-ui
    {{- include "fireseed.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.ui.port }}
      targetPort: {{ .Values.ui.port }}
  selector:
    app: fireseed-ui
